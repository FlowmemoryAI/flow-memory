# Base Sepolia Dry-Run Artifacts

These artifacts are generated for public-alpha/testnet preflight only. They do not deploy contracts, do not require private keys, and use zero-address placeholders until an explicit reviewed testnet deployment path is approved.
