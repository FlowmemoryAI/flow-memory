# Neural GPU Training Lane

The cloud GPU lane remains optional. Local validation does not require CUDA. GPU validation should be run on RunPod/Lambda/Vast/local Linux GPU using `scripts/gpu_env_check.py`, `scripts/cloud_gpu_validate.py`, `scripts/train_neural_gpu.py`, and artifact packaging scripts.

Imported GPU proof is evidence metadata only, not a model-quality guarantee.
