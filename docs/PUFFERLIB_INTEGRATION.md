# PufferLib Integration

PufferLib is a future high-throughput backend seam. Current status: adapter seam only. The base suite lazily imports PufferLib and fails clearly if unavailable. No Puffer code is vendored and no speed claims are made.
