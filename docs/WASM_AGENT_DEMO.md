# WASM / Browser Agent Demo

`wasm/flow-agent-demo/` is a static browser demo path for exported tabular policies. It shows state, selected action, reward, reputation, safety violations, and episode return style data in the browser. It is a prototype and does not run Python, Torch, or neural inference in the browser.
