"""Optional production adapters.

These modules keep heavy dependencies optional. Install extras such as
`flow-memory[ml]`, `flow-memory[memory]`, or `flow-memory[web3]` before using
adapters backed by PyTorch, Qdrant, Neo4j, Redis, or Web3.
"""
