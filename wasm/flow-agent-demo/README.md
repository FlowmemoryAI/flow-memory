# Flow Agent Browser Demo

Static prototype showing a JSON-exported tabular policy selecting advisory actions in a browser. It does not run neural inference and does not bypass Flow Memory safety policy.
